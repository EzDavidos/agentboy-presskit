# WSL / OS junk
*Zone.Identifier
.DS_Store
Thumbs.db

# Originals from the client (raw photos, the 105 MB video, old screenshots).
# Everything the site actually serves lives in assets/ as a processed copy.
_source/
